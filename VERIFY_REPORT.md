# 功能验证说明

在一台 Android 10 真机上对本仓库改动做过实机验证，下面是各项的结论与判据。
测试账号无关注、无动态，所以有两项只验到入口。

| 功能 | 结论 | 判据 |
|---|---|---|
| 直播首页停止自动播放 | 通过 | 直播页顶部横幅停在「直播中」卡片时为静态封面，无滚动弹幕、无静音图标；非轮播切页时段画面像素变化约 0.04% |
| 批量删除动态（拉列表） | 通过 | 日志可见 `DynSpaceReq` / `DynSpaceRsp`，走 App gRPC 成功返回，无 412 |
| 看完自动移出稍后再看 | 通过 | 日志可见移除请求返回 `{"code":0}`，稍后再看列表清空 |
| 评论时间跳转 | 通过 | 点击评论中的时间戳后，播放器控制条进度跳到该时间点 |
| 记忆合集进度 | 通过 | 观看合集中某一集后退出，再从合集第 1 集进入时日志出现 `jump to <aid>`，页面角标切到上次观看那一集 |
| 按关注分组筛选动态 | 仅验入口 | 分组多选框能正确列出账号的关注分组；测试账号无关注，过滤效果未验证 |
| 显示关注时间 | 未验证 | 开关可开、关注列表页正常打开；测试账号无关注，列表为空 |

## 已知限制

- **记忆合集进度**只在打开「整个合集的第 1 集」时触发跳转。分季/分篇的合集里，某一季的第 1 集不是合集第 1 集，不会触发。
- **批量删除动态**的删除动作本身未做实机验证（测试账号没有可删的动态），只验证到列表能正常拉取。
- 升级安装必须使用与上次相同的签名，否则需要先卸载。

## 两个曾经失效项的修复要点

**直播首页顶部大横幅仍自动播放**：原实现只钩了直播卡片列表的 inline 播放管理器，顶部横幅走的是另一条路径。
反编译后定位到横幅卡片持有者里实现播放接口的方法（日志字符串 `realStartPlay: Starting play for `），
用 ReVanced 指纹（返回 `Z`、参数 `[Z]` 加该字符串）钩住，在开关开启且非手动点播时直接返回 false。

**批量删除动态拉列表返回 412**：网页端接口缺 WBI 签名等风控参数会被拦。
改用 App 自己的 gRPC 拉取空间动态，动态 id 取 `extend.dynIdStr`，描述从模块列表里拼。
